# Jobs release gate: short scenarios (3–6)

Run `20260912T182545Z` inside Container run `20260912T174731Z-d74e59e9f79be6b0`, Project `/home/vlcak/Projekty/boxa`, wall clock 91 s.
Codex runtime in use: `0.154.0`. Script: `tests/jobs_gate.py` (issue 08).

| Scenario | Expected (ADR 0037) | Observed | Verdict |
| --- | --- | --- | --- |
| 3a worker crash mid-run | `orphaned`, command tree alive | state `orphaned`, command pid 10420 alive=True | PASS |
| 3a adopt → outcome | `finished-unknown`, exit code unknown, output kept | adopt rc=0, state `finished-unknown`, exitCode None, stdout `survived-the-worker` | PASS |
| 3b cancel with `setsid` escapee | `cancelled`; the escapee is killed too (tracked via the subreaper) | state `cancelled`, sleeps before=2 after=0, killed=2 | PASS |
| 3c cancel with an `env -i` child | `cancelled`; the marker-less child is killed while the worker lives | state `cancelled`, sleeps before=2 after=0 | PASS |
| 3d cancel of an orphaned Job with an `env -i` child | `cancelled`; the marker-less child may survive and the CLI states the limit | state `cancelled`, marker-less sleep survivors=0, untrackable=0, limit stated=True | PASS |
| 3e Job that starts an inner Docker container | `cancelled`; the container is outside the contract and the CLI says so | inner container seen=True, state `cancelled`, container still running after cancel=True, limit stated=True | PASS |
| 4a first Codex job probes and publishes the current version | `0.154.0` verified with a probe, job runs from `<versions>/0.154.0/` | version `0.154.0`, source `npm-volume`, verified=True probed=True | PASS |
| 4b Codex update between two jobs: the new job probes the new version | second job runs from `<versions>/0.154.1/` (probed, verified), first keeps `0.154.0` | rc=0, version `0.154.1`, `0.154.1` verified=True probed=True | PASS |
| 4b the old job finished on its own copy | `done`, binary still under `0.154.0/`, final message SLEPT | state `done`, binary under `0.154.0/`, message `SLEPT` | PASS |
| 4b the new job finished on the new copy | `done`, binary under `0.154.1/` | state `done`, message `UPDATED` | PASS |
| 4c `npm` update concurrent with the runtime copy | copy of `0.154.2` discarded, `0.154.1` stays in use, loud warning | `0.154.2` verified=False, in use `0.154.1`, warnings=1 | PASS |
| 4c no snapshot temp dir leaked | no `.snapshot-*` under the versions root | leftovers=[] | PASS |
| 6a non-zero exit | `failed`, exit code 3 | state `failed`, exitCode 3 | PASS |
| 6b large stdout (30 MB) | `done`, full stdout on disk, `log --tail` answers | state `done`, stdout 30.0 MB, tail ok=True | PASS |
| 6c cancel mid-run | `cancelled` (never `failed`); signal exit recorded | state `cancelled`, exitCode -15 | PASS |
| 6d/2 duplicate start with the same key and request | `attached` to the same jobId, no second run | result `attached`, same id=True | PASS |
| 6d same key, different request | `conflict`, exit 6 | rc=6, result `conflict` | PASS |

## Notes

- `crash`: needs-ack for 20260912T180012-oed9g6; acked (gate-driven parallelism)
- `setsid`: needs-ack for 20260912T180012-oed9g6; acked (gate-driven parallelism)
- `env-i`: needs-ack for 20260912T180012-oed9g6; acked (gate-driven parallelism)
- `env-i-orphan`: needs-ack for 20260912T180012-oed9g6; acked (gate-driven parallelism)
- `docker-inner`: needs-ack for 20260912T180012-oed9g6; acked (gate-driven parallelism)
- 3e: inner container `gate-inner-20260912t182545z` survived the cancel (the `docker run` client was killed; the daemon-side container is the documented limit). Removed by the gate.
- `codex-a`: needs-ack for 20260912T180012-oed9g6; acked (gate-driven parallelism)
- `codex-b`: needs-ack for 20260912T180012-oed9g6,20260912T182618-ck65ze; acked (gate-driven parallelism)
- 4b: the runtime refresh runs before the concurrency check, so the `needs-ack` refusal of the first start had already copied and probed the new version; the acked retry took the fast path (`runtimeProbed: false`). One probe per version either way, but a refused start pays it.
- 4c warning text: codex runtime: snapshot of 0.154.2 from npm-volume discarded (source changed during the copy); previous verified runtime stays in use
- 5: Container restart cannot be driven from inside; no `interrupted` record in this Project. Host proof: issue 06 comments (stop + start → `interrupted`, reason `container-restart`).
- `exit3`: needs-ack for 20260912T180012-oed9g6; acked (gate-driven parallelism)
- `big-stdout`: needs-ack for 20260912T180012-oed9g6; acked (gate-driven parallelism)
- `cancel-mid`: needs-ack for 20260912T180012-oed9g6; acked (gate-driven parallelism)
- `dup`: needs-ack for 20260912T180012-oed9g6; acked (gate-driven parallelism)

**17 PASS, 0 FAIL.**

---

## Long run (scenarios 1–2)

Job `20260912T180012-oed9g6`, key `gate/long/two-hour`, model `gpt-5.6-luna` effort `low`, Codex `0.154.0`.

| Check | Observed | Verdict |
| --- | --- | --- |
| the reported Job is the one `long start` recorded | reported `20260912T180012-oed9g6`, recorded `20260912T180012-oed9g6` | PASS |
| state `done`, exit 0, terminal event `turn.completed` | `done`, exit 0, `turn.completed` | PASS |
| exactly one Job under the key since `long start` (no second run from wait expiry or reconnect) | 1 Job(s): 20260912T180012-oed9g6 | PASS |
| ran at least 2.00 h (the sleep was not shortened) | 120.3 min | PASS |
| final message is the fixture's sha256 | expected `400f0a7d1f0d9be3d30fcd05f7db19abe723fdc81da589a79a33a47e3d0a5db4`, got `400f0a7d1f0d9be3d30fcd05f7db19abe723fdc81da589a79a33a47e3d0a5db4` | PASS |

| Baseline | Value |
| --- | --- |
| Codex usage | {"cache_write_input_tokens": 0, "cached_input_tokens": 679168, "input_tokens": 740722, "output_tokens": 2528, "reasoning_output_tokens": 903} |
| item counts | {"agent_message": 2, "command_execution": 1} |
| wait calls, subagent 1 (ended on purpose after 3) | 3, 1637 s, 288 B stdout, 21 101 tokens (Agent tool usage) |
| wait calls, subagent 2 (reconnect via long attach, to the end) | 11, 5517 s, 4 249 B stdout (10 x 98 B running + 3 269 B final), 25 463 tokens |
| wait calls total | 14 over 7 219 s; running-status output 1 268 B; expiry never started a second Job |
| waiting-agent tokens vs Codex usage | Claude shells 46 564 tokens total (Agent tool usage; no cost field exposed) vs Codex input 740 722 (679 168 cached) / output 2 528 (903 reasoning) on gpt-5.6-luna |
| interim Codex job beside the run | ccode + cr review + 2 re-review Jobs and 3 short gate runs used --ack-concurrent on this Job; none disturbed it |

**5 PASS, 0 FAIL.**
