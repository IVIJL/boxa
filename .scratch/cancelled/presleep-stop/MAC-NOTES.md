# macOS-deferred work

Issues that require a macOS machine to implement or verify. One
subdirectory per feature (`.scratch/mac/<feature-slug>/`), same issue
file format as the regular tracker. Work here stays
`Status: ready-for-human` until a session actually runs on the user's
Mac; then it is triaged and finished there.
